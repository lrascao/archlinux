---

- name: install k8s from AUR
  aur: name={{ item }} user={{ archlinux_username }}
  with_items:
    - virtualbox
    - virtualbox-bin
    - virtualbox-host-modules-arch
    - kubectl
    - minikube

